# Python
__pycache__/
*.py[cod]
.venv/
.env
.env.*
!.env.example

# uv
.uv-cache/

# Generated audio (large, regenerable)
audio/
*.wav
*.mp3
!.gitkeep

# Per-run scratch (rewrite IO etc.)
work/

# Pre-Mongo state directories — Mongo + MinIO now hold all metadata + bytes.
# These only exist if the old JSON-backed code is still in use somewhere.
jobs/
feed/

# Local test inputs you don't want committed
test-input.md
*.test.md

# Generated feed (regenerable from audio + metadata)
feed/feed.xml

# macOS
.DS_Store

# Local config (user-specific source paths, secrets)
.local/
CLAUDE.local.md
